/*
A05. Второе Значение
ограничение по времени на тест1 секунда
ограничение по памяти на тест256 мегабайт
Дано N целых неотрицательных чисел. Нужно найти второе по величине значение, встречающееся в данном множестве (то есть значение, которое станет наибольшим, если удалить все элементы, равные текущему максимуму).

Входные данные
В первой строке содержится единственное число N — количество чисел (1 ≤ N ≤ 100). Во второй строке заданы N целых чисел ai, разделенные пробелами (0 ≤ ai ≤ 1000).

Выходные данные
Вывести второе по величине значение, встречающееся в данном множестве. Если в множестве нет второго по величине значения (все элементы равны) — нужно вывести -1.
*/


#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector <short> arr;
    int n;
    cin >> n;


    short maxInd = 0;
    short max = 0, secondMax = -1, x;
    for (short i = 0; i < n; i++)
    {
        cin >> x;
        arr.push_back(x);
        if (max < x) {
            max = x;
            maxInd = i;
        }
    }
   
    for (short i = 0; i < n; i++)
    {
        if (secondMax < arr[i] and arr[i] != arr[maxInd])
        {
            secondMax = arr[i];
        }
    }

    printf("%d", secondMax);
}
