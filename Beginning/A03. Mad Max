/*
A03. Mad Max
ограничение по времени на тест1 секунда
ограничение по памяти на тест256 мегабайт
Дано N целых неотрицательных чисел. Найдите максимальный элемент и количество чисел в данном наборе, равных этому максимальному элементу.

Входные данные
В первой строке содержится единственное число N — количество чисел (1 ≤ N ≤ 100). Во второй строке заданы N целых чисел ai, разделенные пробелами (0 ≤ ai ≤ 1000).

Выходные данные
Вывести два числа — максимальный элемент среди данных и количество чисел в наборе, равных этому элементу.
*/


#include <iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;

    short max = 0, count = 0, x;
    for (short i = 0; i < n; i++)
    {
        cin >> x;
        
        if (max < x) {
            max = x;
            count = 1;
        }
        else if (max == x)
        {
            count++;
        }
        
    }

    printf("%d %d",max, count);
}
