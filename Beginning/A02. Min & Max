/*
A02. Min & Max
ограничение по времени на тест1 секунда
ограничение по памяти на тест256 мегабайт
Дано N целых неотрицательных чисел. Нужно вывести минимальное и максимальное число среди них.

Входные данные
В первой строке содержится единственное число N — количество чисел (1 ≤ N ≤ 100). Во второй строке заданы N целых чисел ai, разделенные пробелами (0 ≤ ai ≤ 1000).

Выходные данные
Вывести минимальное и максимальное число в данном наборе.
*/


#include <iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;

    short max = 0, min = 1000, x;
    for (short i = 0; i < n; i++)
    {
        cin >> x;
        
        if (max < x) max = x;
        if (min > x) min = x;
    }

    printf("%d %d", min,max);
}
